# 反馈问题前请准备

## 建议最少提供这些信息

- 操作系统和版本
- OmniInfer / OmniStudio 版本或提交号
- backend ID
- 模型名称与量化
- 是否使用 `mmproj`
- 完整复现步骤
- CLI 输出
- `~/.cache/omniinfer/gateway.log` 关键片段
- runtime 日志关键片段
- `/health` 或 `/omni/state` 返回结果

## 一条高质量问题反馈的目标

让维护者不需要反复追问，就能马上复现或缩小问题范围。
